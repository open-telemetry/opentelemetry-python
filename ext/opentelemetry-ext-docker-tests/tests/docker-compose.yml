version: '3'

services:
  otmongo:
    ports:
     - "27017:27017"
    image: mongo:latest

  otpostgres:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpassword
      POSTGRES_DB: opentelemetry-tests