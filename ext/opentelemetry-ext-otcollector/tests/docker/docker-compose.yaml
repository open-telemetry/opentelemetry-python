version: "2"
services:

  # Collector
  collector:
    image: omnition/opentelemetry-collector:latest
    command: ["--config=/config/collector-config.yaml", "--log-level=DEBUG"]
    volumes:
      - ./collector-config.yaml:/etc/collector-config.yaml
    ports:
      - "55678:55678"
