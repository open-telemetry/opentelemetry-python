`opentelemetry-propagator-jaeger`: fix typing issues and enable pyright typechecking for the package
`opentelemetry-propagator-jaeger`: skip `uberctx-` baggage headers with an empty value on extraction instead of raising `TypeError`
